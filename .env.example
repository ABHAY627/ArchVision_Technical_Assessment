# PostgreSQL connection string (e.g. from Neon, Supabase, or local)
DATABASE_URL=

# "pollinations" (free, no key) or "gemini" (requires GEMINI_API_KEY)
AI_PROVIDER=pollinations

# Only required if AI_PROVIDER=gemini
GEMINI_API_KEY=
